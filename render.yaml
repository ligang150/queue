services:
  - type: web
    name: queue-system
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: CLIENT_ID
        sync: false
      - key: ACCESS_TOKEN
        sync: false
      - key: OPEN_ID
        sync: false
